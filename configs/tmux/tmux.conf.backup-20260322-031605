# --------------------------------------------------- #
#             CONFIG TMUX TỐI GIẢN VÀ ỔN ĐỊNH             #
# --------------------------------------------------- #

# Đặt prefix là Ctrl+a
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Hỗ trợ chuột
set -g mouse on

# Bắt đầu đánh số cửa sổ và pane từ 1
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# Hỗ trợ màu sắc đầy đủ
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# --------------------------------------------------- #
#                   DANH SÁCH PLUGIN (TPM)            #
# --------------------------------------------------- #
set -g @plugin 'tmux-plugins/tpm'

# Theme chính của Omarchy
set -g @plugin 'joaofelipegalvao/omarchy-tmux'

# Các plugin hữu ích khác
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'joshmedeski/tmux-fzf-url'

# --------------------------------------------------- #
#                    KHỞI ĐỘNG TPM                    #
# --------------------------------------------------- #
# Luôn đặt dòng này ở cuối file
run '~/.config/tmux/plugins/tpm/tpm'
