diff --git c/bin/omabackup w/bin/omabackup
index 8ad4457..c46cb47 100755
--- c/bin/omabackup
+++ w/bin/omabackup
@@ -944,7 +944,7 @@ cmd_collect() {
                 # it. Anything else -- rsync could not even start, a
                 # destination it could not write to at all -- is not that
                 # case, and is refused rather than counted as collected.
-                rsync -a --exclude '.git/' "${ex[@]}" "$e/" "$dest/"; rc=$?
+                rsync -a --exclude '.git' --exclude '.git/**' "${ex[@]}" "$e/" "$dest/"; rc=$?
                 (( rc == 0 || rc == 23 || rc == 24 )) \
                     || die "could not copy $(_tilde "$e") into staging"
             else
diff --git c/groups.default.json w/groups.default.json
index 03d4cc9..973ec16 100644
--- c/groups.default.json
+++ w/groups.default.json
@@ -70,7 +70,8 @@
         "~/.local/state/omarchy/toggles",
         "~/.local/state/omarchy/migrations",
         "~/.local/state/omarchy/current/theme.name",
-        "~/.local/state/omarchy/current/background"
+        "~/.local/state/omarchy/current/background",
+        "~/.local/state/omarchy/workspace-layouts"
       ],
       "probe": "migrations"
     },
diff --git c/lib/publish.sh w/lib/publish.sh
index 13ddd90..23c4eab 100644
--- c/lib/publish.sh
+++ w/lib/publish.sh
@@ -100,7 +100,7 @@ _publish_file() {  # _publish_file <src> <dst> <repo-root> [<repo-root-rp>]
     # structural corruption of the repo rather than the intended overwrite.
     # Refused, not guessed at: this function does not know whether that
     # directory is meant to be there.
-    [[ ! -d "$dst" ]] || return 1
+    [[ -L "$dst" || ! -d "$dst" ]] || return 1
     mkdir -p "$(dirname "$dst")" || return 1
     # A symlink is content: copied as a link, never followed. Publishing what it
     # points at would silently turn a link into a fat copy of somebody else's
@@ -236,7 +236,7 @@ publish_staging() {
             continue
         fi
         rels+=("$rel"); dsts+=("$dst")
-    done < <(find "$staging" \( -type f -o -type l \) -print0 2>/dev/null)
+    done < <(find "$staging" \( -type f -o -type l \) -not -name ".git" -not -path "*/.git/*" -print0 2>/dev/null)
     # find's own status, checked BEFORE anything below ever runs -- the
     # earlier version incremented `failed` here but fell through to the
     # write pass regardless. A PoC (a stub find that prints one file then
@@ -422,7 +422,7 @@ publish_staging() {
                 # whether rsync actually put it there.
                 printf '%s\0' "${pfiles[@]}" >"$pflist" \
                     || { rm -f "$pflist"; failed=$((failed + 1)); continue; }
-                rsync -a --exclude '.git/' --files-from="$pflist" --from0 \
+                rsync -a --exclude '.git' --exclude '.git/**' --files-from="$pflist" --from0 \
                     "$f" "$repo/configs/omarchy/plugins/$pid/"
                 local rsync_rc=$?
                 rm -f "$pflist"
diff --git c/secrets.deny.json w/secrets.deny.json
index 23386a4..6452290 100644
--- c/secrets.deny.json
+++ w/secrets.deny.json
@@ -1,7 +1,7 @@
 {
   "schemaVersion": 1,
   "_comment": [
-    "A deny-list scanned over what is about to leave the machine (docs/DESIGN.md §6).",
+    "A deny-list scanned over what is about to leave the machine (docs/DESIGN.md \u00a76).",
     "A hit BLOCKS the push -- it does not warn. A leak is irreversible, and a",
     "warning nobody reads is lesson #1 of this whole project.",
     "",
@@ -47,8 +47,8 @@
     },
     {
       "id": "pgp-private-block",
-      "regex": "-----BEGIN PGP PRIVATE KEY BLOCK-----",
-      "reason": "PGP secret key material."
+      "regex": "-----BEGIN PGP PRIVATE KEY BLO[C]K-----",
+      "reason": "PGP secret key material. Character class [C] keeps the rule equivalent while the deny-list file itself does not contain the contiguous header literal (so backing up this plugin no longer false-positives)."
     },
     {
       "id": "putty-private-key",
@@ -62,5 +62,36 @@
       "reason": "A long literal assigned to something named like a credential. Quoted, 16+ chars, and not starting with $ or a backtick -- so a variable reference or command substitution reads as indirection, not as the secret itself."
     }
   ],
-  "exceptions": []
+  "exceptions": [
+    {
+      "id": "tmux-powerkit-sk-ant",
+      "match": "sk-ant-oat11-dummy-fixture-only-000000000000",
+      "reason": "Dummy strings in tmux-powerkit bats fixtures (configs/tmux/plugins/tmux-powerkit/tests/aiquotas.bats); labeled dummy-fixture-only, not real API keys."
+    },
+    {
+      "id": "tmux-powerkit-sk-oai-admin",
+      "match": "sk-oai-admin-dummy-fixture-only-000000000000",
+      "reason": "Dummy strings in tmux-powerkit bats fixtures (configs/tmux/plugins/tmux-powerkit/tests/aiquotas.bats); labeled dummy-fixture-only, not real API keys."
+    },
+    {
+      "id": "tmux-powerkit-sk-openai",
+      "match": "sk-openai-dummy-fixture-only-00000000",
+      "reason": "Dummy strings in tmux-powerkit bats fixtures (configs/tmux/plugins/tmux-powerkit/tests/aiquotas.bats); labeled dummy-fixture-only, not real API keys."
+    },
+    {
+      "id": "tmux-powerkit-sk-ds",
+      "match": "sk-ds-dummy-fixture-only-0000000000000000",
+      "reason": "Dummy strings in tmux-powerkit bats fixtures (configs/tmux/plugins/tmux-powerkit/tests/aiquotas.bats); labeled dummy-fixture-only, not real API keys."
+    },
+    {
+      "id": "tmux-powerkit-sk-zai",
+      "match": "sk-zai-dummy-fixture-only-0000000000000000",
+      "reason": "Dummy strings in tmux-powerkit bats fixtures (configs/tmux/plugins/tmux-powerkit/tests/aiquotas.bats); labeled dummy-fixture-only, not real API keys."
+    },
+    {
+      "id": "tmux-powerkit-sk-kimi",
+      "match": "sk-kimi-dummy-fixture-only-0000000000000000",
+      "reason": "Dummy strings in tmux-powerkit bats fixtures (configs/tmux/plugins/tmux-powerkit/tests/aiquotas.bats); labeled dummy-fixture-only, not real API keys."
+    }
+  ]
 }
