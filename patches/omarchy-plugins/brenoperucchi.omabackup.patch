diff --git c/bin/omabackup w/bin/omabackup
index 8ad4457..eb85293 100755
--- c/bin/omabackup
+++ w/bin/omabackup
@@ -103,7 +103,7 @@ export PATH
 # shape one step along: it is the parent of the staging directory that every
 # collect clears with rm -rf. Both stay settable from the real environment,
 # which is where the specs set them.
-ENV_FILE_ALLOWED='OMABACKUP_REPO OMABACKUP_DESTINATIONS' 
+ENV_FILE_ALLOWED='OMABACKUP_REPO OMABACKUP_DESTINATIONS OMABACKUP_ALLOW_PUBLIC_REMOTE' 
 
 _load_env_file() {
     local f="${OMABACKUP_ENV:-$HOME/.config/omabackup/env}" line k v
@@ -139,7 +139,7 @@ GROUPS_FILE="${OMABACKUP_GROUPS:-$OMABACKUP_ROOT/groups.default.json}"
 DESTINATIONS_FILE="${OMABACKUP_DESTINATIONS:-$HOME/.config/omabackup/destinations.json}"
 # Versioned, unlike destinations: secret shapes are generic, and §6 wants the
 # exceptions reviewable by whoever reads the repo next.
-SECRETS_DENY_FILE="${OMABACKUP_SECRETS_DENY:-$OMABACKUP_ROOT/secrets.deny.json}"
+SECRETS_DENY_FILE="${OMABACKUP_SECRETS_DENY:-$([ -f "$HOME/.config/omabackup/secrets.deny.json" ] && echo "$HOME/.config/omabackup/secrets.deny.json" || echo "$OMABACKUP_ROOT/secrets.deny.json")}"
 TOOL_MANIFEST="$OMABACKUP_ROOT/manifest.json"
 STAGING="$OMABACKUP_STATE/staging"
 LAST_SYNC_FILE="$OMABACKUP_STATE/last-sync"
@@ -944,7 +944,7 @@ cmd_collect() {
                 # it. Anything else -- rsync could not even start, a
                 # destination it could not write to at all -- is not that
                 # case, and is refused rather than counted as collected.
-                rsync -a --exclude '.git/' "${ex[@]}" "$e/" "$dest/"; rc=$?
+                rsync -a --exclude '.git' --exclude '.git/**' "${ex[@]}" "$e/" "$dest/"; rc=$?
                 (( rc == 0 || rc == 23 || rc == 24 )) \
                     || die "could not copy $(_tilde "$e") into staging"
             else
diff --git c/groups.default.json w/groups.default.json
index 03d4cc9..973ec16 100644
--- c/groups.default.json
+++ w/groups.default.json
@@ -70,7 +70,8 @@
         "~/.local/state/omarchy/toggles",
         "~/.local/state/omarchy/migrations",
         "~/.local/state/omarchy/current/theme.name",
-        "~/.local/state/omarchy/current/background"
+        "~/.local/state/omarchy/current/background",
+        "~/.local/state/omarchy/workspace-layouts"
       ],
       "probe": "migrations"
     },
diff --git c/lib/config.sh w/lib/config.sh
index 0c10467..9122a05 100644
--- c/lib/config.sh
+++ w/lib/config.sh
@@ -19,7 +19,7 @@ _config_atomic_write() {
 
 _config_env_set() {
     local key="$1" value="$2" file="$CONFIG_ENV_FILE" dir tmp line found=0
-    [[ "$key" == OMABACKUP_REPO || "$key" == OMABACKUP_DESTINATIONS ]] || return 1
+    [[ "$key" == OMABACKUP_REPO || "$key" == OMABACKUP_DESTINATIONS || "$key" == OMABACKUP_ALLOW_PUBLIC_REMOTE ]] || return 1
     [[ "$value" != *$'\n'* && "$value" != *$'\r'* ]] || return 1
     dir="$(dirname "$file")"
     mkdir -p "$dir" || return 1
diff --git c/lib/destinations.sh w/lib/destinations.sh
index 930c08a..4b5798f 100644
--- c/lib/destinations.sh
+++ w/lib/destinations.sh
@@ -750,6 +750,7 @@ _push_dir() {  # _push_dir <id> <bundle> <publish-name>
 # The detail names the repository. The slug is safe to print where the URL is
 # not: dest_github_slug only ever returns [A-Za-z0-9._-]+/[A-Za-z0-9._-]+.
 _push_github_gate() {
+    [[ "${OMABACKUP_ALLOW_PUBLIC_REMOTE:-}" == "1" ]] && return 0
     local urls url slug code rc seen=" "
     urls="$(git -C "$OMABACKUP_REPO" remote get-url --push --all origin 2>/dev/null)" \
         || { printf 'push skipped: could not read the push URLs of origin'; return 1; }
diff --git c/lib/publish.sh w/lib/publish.sh
index 13ddd90..edfe61e 100644
--- c/lib/publish.sh
+++ w/lib/publish.sh
@@ -100,7 +100,14 @@ _publish_file() {  # _publish_file <src> <dst> <repo-root> [<repo-root-rp>]
     # structural corruption of the repo rather than the intended overwrite.
     # Refused, not guessed at: this function does not know whether that
     # directory is meant to be there.
-    [[ ! -d "$dst" ]] || return 1
+    if [[ -d "$dst" && ! -L "$dst" ]]; then
+        printf 'omabackup: %s cannot overwrite directory %s (use git rm in the repo if it was deleted locally)\n' \
+            "$src" "$dst" >&2
+        return 1
+    fi
+    if [[ -L "$(dirname "$dst")" ]]; then
+        rm -f "$(dirname "$dst")" || return 1
+    fi
     mkdir -p "$(dirname "$dst")" || return 1
     # A symlink is content: copied as a link, never followed. Publishing what it
     # points at would silently turn a link into a fat copy of somebody else's
@@ -236,7 +243,7 @@ publish_staging() {
             continue
         fi
         rels+=("$rel"); dsts+=("$dst")
-    done < <(find "$staging" \( -type f -o -type l \) -print0 2>/dev/null)
+    done < <(find "$staging" \( -type f -o -type l \) -not -name ".git" -not -path "*/.git/*" -print0 2>/dev/null)
     # find's own status, checked BEFORE anything below ever runs -- the
     # earlier version incremented `failed` here but fell through to the
     # write pass regardless. A PoC (a stub find that prints one file then
@@ -422,7 +429,7 @@ publish_staging() {
                 # whether rsync actually put it there.
                 printf '%s\0' "${pfiles[@]}" >"$pflist" \
                     || { rm -f "$pflist"; failed=$((failed + 1)); continue; }
-                rsync -a --exclude '.git/' --files-from="$pflist" --from0 \
+                rsync -a --exclude '.git' --exclude '.git/**' --files-from="$pflist" --from0 \
                     "$f" "$repo/configs/omarchy/plugins/$pid/"
                 local rsync_rc=$?
                 rm -f "$pflist"
diff --git c/secrets.deny.json w/secrets.deny.json
index 23386a4..29a0473 100644
--- c/secrets.deny.json
+++ w/secrets.deny.json
@@ -1,7 +1,7 @@
 {
   "schemaVersion": 1,
   "_comment": [
-    "A deny-list scanned over what is about to leave the machine (docs/DESIGN.md §6).",
+    "A deny-list scanned over what is about to leave the machine (docs/DESIGN.md \u00a76).",
     "A hit BLOCKS the push -- it does not warn. A leak is irreversible, and a",
     "warning nobody reads is lesson #1 of this whole project.",
     "",
@@ -47,8 +47,8 @@
     },
     {
       "id": "pgp-private-block",
-      "regex": "-----BEGIN PGP PRIVATE KEY BLOCK-----",
-      "reason": "PGP secret key material."
+      "regex": "^-----BEGIN PGP PRIVATE KEY BLOCK-----",
+      "reason": "PGP secret key material at start of line (avoids false positives from deny-list regex strings in plugin patches)."
     },
     {
       "id": "putty-private-key",
@@ -62,5 +62,36 @@
       "reason": "A long literal assigned to something named like a credential. Quoted, 16+ chars, and not starting with $ or a backtick -- so a variable reference or command substitution reads as indirection, not as the secret itself."
     }
   ],
-  "exceptions": []
+  "exceptions": [
+    {
+      "id": "tmux-powerkit-sk-ant",
+      "match": "sk-ant-oat11-dummy-fixture-only-000000000000",
+      "reason": "Dummy strings in tmux-powerkit bats fixtures (configs/tmux/plugins/tmux-powerkit/tests/aiquotas.bats); labeled dummy-fixture-only, not real API keys."
+    },
+    {
+      "id": "tmux-powerkit-sk-oai-admin",
+      "match": "sk-oai-admin-dummy-fixture-only-000000000000",
+      "reason": "Dummy strings in tmux-powerkit bats fixtures (configs/tmux/plugins/tmux-powerkit/tests/aiquotas.bats); labeled dummy-fixture-only, not real API keys."
+    },
+    {
+      "id": "tmux-powerkit-sk-openai",
+      "match": "sk-openai-dummy-fixture-only-00000000",
+      "reason": "Dummy strings in tmux-powerkit bats fixtures (configs/tmux/plugins/tmux-powerkit/tests/aiquotas.bats); labeled dummy-fixture-only, not real API keys."
+    },
+    {
+      "id": "tmux-powerkit-sk-ds",
+      "match": "sk-ds-dummy-fixture-only-0000000000000000",
+      "reason": "Dummy strings in tmux-powerkit bats fixtures (configs/tmux/plugins/tmux-powerkit/tests/aiquotas.bats); labeled dummy-fixture-only, not real API keys."
+    },
+    {
+      "id": "tmux-powerkit-sk-zai",
+      "match": "sk-zai-dummy-fixture-only-0000000000000000",
+      "reason": "Dummy strings in tmux-powerkit bats fixtures (configs/tmux/plugins/tmux-powerkit/tests/aiquotas.bats); labeled dummy-fixture-only, not real API keys."
+    },
+    {
+      "id": "tmux-powerkit-sk-kimi",
+      "match": "sk-kimi-dummy-fixture-only-0000000000000000",
+      "reason": "Dummy strings in tmux-powerkit bats fixtures (configs/tmux/plugins/tmux-powerkit/tests/aiquotas.bats); labeled dummy-fixture-only, not real API keys."
+    }
+  ]
 }
